---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app budget-app
  namespace: &namespace budget
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      main:
        annotations:
          reloader.stakater.com/auto: "true"
        replicas: 2
        strategy: RollingUpdate
        pod:
          topologySpreadConstraints:
            - maxSkew: 1
              topologyKey: kubernetes.io/hostname
              whenUnsatisfiable: ScheduleAnyway
              labelSelector:
                matchLabels:
                  app.kubernetes.io/name: *app
                  app.kubernetes.io/instance: *app
        initContainers:
          01-init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: "18"
            envFrom:
              - secretRef:
                  name: budget-initdb-secret
          02-migrate:
            image:
              repository: ghcr.io/pipitonelabs/budget-app
              # Digest-pinned: Renovate bumps this on each new migrate-latest push.
              tag: migrate-latest@sha256:f8a6ad7d8de0ad4994824b92fa2c48aea2349db10bacffb3e138cb3d776adda3
            env:
              DATABASE_URL:
                valueFrom:
                  secretKeyRef:
                    name: budget-pguser-secret
                    key: uri
        containers:
          app:
            image:
              repository: ghcr.io/pipitonelabs/budget-app
              # Digest-pinned: Renovate bumps this on each new latest push.
              tag: latest@sha256:96952aae5df6d5031e0ab17c5191f7146997cccfc3ea7f63ffebfd41503550c0
            env:
              DATABASE_URL:
                valueFrom:
                  secretKeyRef:
                    name: budget-pguser-secret
                    key: uri
              AUTH_URL: "https://budget.pipitonelabs.com"
              AUTH_TRUST_HOST: "true"
              NODE_ENV: production
              PORT: "3000"
              HOSTNAME: "0.0.0.0"
            envFrom:
              - secretRef:
                  name: budget-app-secret
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: 3000
                  initialDelaySeconds: 10
                  periodSeconds: 30
                  failureThreshold: 3
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /healthz
                    port: 3000
                  initialDelaySeconds: 5
                  periodSeconds: 10
                  failureThreshold: 3
            resources:
              requests:
                cpu: 10m
                memory: 128Mi
              limits:
                memory: 512Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities:
                drop: ["ALL"]
    defaultPodOptions:
      imagePullSecrets:
        - name: ghcr-login
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
    service:
      app:
        controller: main
        ports:
          http:
            port: 3000
